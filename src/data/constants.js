/**
 * Constantes de l'application
 */

/**
 * Version actuelle de l'application
 * @type {string}
 */
export const VERSION = "1.0.0";

/**
 * Date de dernière mise à jour
 * @type {string}
 */
export const LAST_UPDATE = new Date().toISOString().split("T")[0];

/**
 * URL de base du site original
 * @type {string}
 */
export const BASE_URL = "https://micetf.fr";

/**
 * Chemin de base pour les miniatures (dossier public)
 * @type {string}
 */
export const THUMBNAIL_PATH = "/assets/thumbnails/";

/**
 * Chemin vers l'image par défaut (dossier public)
 * @type {string}
 */
export const DEFAULT_IMAGE = "/assets/placeholder.png";
